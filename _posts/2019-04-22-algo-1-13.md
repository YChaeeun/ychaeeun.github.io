---
title: algo lev-1 문자열 내 마음대로 정렬하기 (python)
date: 2019-04-22
published: false
categories:
- algorithm
---



> 문자열로 구성된 리스트 strings와, 정수 n이 주어졌을 때, 각 문자열의 인덱스 n번째 글자를 기준으로 오름차순 정렬하려 합니다. 예를 들어 strings가 [sun, bed, car]이고 n이 1이면 각 단어의 인덱스 1의 문자 u, e, a로 strings를 정렬합니다.
>
> (제한: string은 길이 1이상 50이하 / 원소들은 소문자로 이루어져 있고, 길이 1 이상 100이하 / 모든 string의 원소의 길이는 n보다 크다 / 인덱스 1의 문자가 같은 원소가 여러개일 경우, 사전순으로 앞선 문자열이 앞쪽에! )
>
> 
>
> 출처 : [프로그래머스 Level1](https://programmers.co.kr/learn/challenges?tab=all_challenges)





## 내가 푼 풀이



안됨... 으으!

```python
def solution(strings, n):
    answer = list()
    for idx in strings:
        answer.append(idx[n:n+1])
    return answer
```



```python
def solution(strings, n):
    answer = []
    list1 = [char[n:] for char in strings]
    list1.sort()

    charlist = {char[n:] : idx for idx, char in enumerate(strings)}


    tmp = []
    charlast = list1[-1]
    for index in range(1, len(list1)) :
        char = list1[index-1]
        char2 = list1[index]
        idx = strings[charlist[char]]
        
        if char[:1] == char2[:1] :
            if char2 == charlast :
                tmp.append(idx[:n]+char2)
            tmp.append(idx[:n]+char)
            continue
        
            
        else :
            if tmp :
                answer.append(''.join(sorted(tmp)))
                tmp = []
            answer.append(idx[:n]+char)
            
            
    if list1[-1][:1] != list1[-2][:1] :
        idx = strings[charlist[list1[-1]]]
        answer.append(idx[:n]+list1[-1])

    return answer
```







```python
def solution(strings, n):
    answer = []
    list1 = [char[n:] for char in strings]
    list1.sort()

    charlist = {char[n:] : idx for idx, char in enumerate(strings)}


    tmp = []
    charlast = list1[-1]
    for index in range(1, len(list1)) :
        char = list1[index-1]
        char2 = list1[index]
        idx = strings[charlist[char]]
        
        if char[:1] == char2[:1] :
            if char2 == charlast :
                tmp.append(idx[:n]+char2)
            tmp.append(idx[:n]+char)
            continue
        
            
        else :
            if tmp :
                answer.append(''.join(sorted(tmp)))
                tmp = []
            answer.append(idx[:n]+char)
            
            
    return answer
```



자꾸 에러가 나 왜???

```c
def solution(strings, n):
    answer = []
    list1 = [char[n:] for char in strings]
    list1.sort()

    charlist = {char[n:] : idx for idx, char in enumerate(strings)}


    tmp = []
    charlast = list1[-1]
    for index in range(1, len(list1)) :
        char = list1[index-1]
        char2 = list1[index]
        idx = strings[charlist[char]]
        idx2 = strings[charlist[char2]]
        
        
        ## 이 부분... 흐음...
        ## tmp[-1:] 이라고 하면 밑에서 인덱스 에러가 남.. 왜??
        if tmp[:-1] == [idx[:n]+char] :
            continue
        ## 이 부분때문에 에러남!!!!!
        
        elif char[:1] == char2[:1] :
            tmp.append(idx[:n]+char)
            tmp.append(idx2[:n]+char2)
            
            continue
            
        else :
            if tmp :
                print(tmp)
                tmp.sort()
                answer.extend(tmp)
                tmp = []
            answer.append(idx[:n]+char)
            
    if answer[-1][n:] != list1[-1] : 
        idx = strings[charlist[list1[-1]]]
        answer.append(idx[:n]+list1[-1])

    return answer
```





e되나???

 ```c
def solution(strings, n):
    answer = []
    list1 = [char[n:] for char in strings]
    list1.sort()

    charlist = {char[n:] : idx for idx, char in enumerate(strings)}


    tmp = []
    charlast = list1[-1]
    for index in range(1, len(list1)) :
        char = list1[index-1]
        char2 = list1[index]
        idx = strings[charlist[char]]
        idx2 = strings[charlist[char2]]
        
    
        
        if char[:1] == char2[:1] :
            tmp.append(idx[:n]+char)
            tmp.append(idx2[:n]+char2)
            
            continue
            
        else :
            if tmp :
                print(1111, tmp)
                tmp.sort()
                print(tmp)
                answer.extend(tmp)
                tmp = []
            else :
                answer.append(idx[:n]+char)
            
            
    if answer[-1][n:] != list1[-1] : 
        idx = strings[charlist[list1[-1]]]
        answer.append(idx[:n]+list1[-1])

    return answer
 ```



후... 이제 set 해서 뭉뚱그려 한 부분만 해결하면 돼

```c
def solution(strings, n):
    answer = []
    list1 = [char[n:] for char in strings]
    list1.sort()

    charlist = {char[n:] : idx for idx, char in enumerate(strings)}


    tmp = []
    charlast = list1[-1]
    for index in range(1, len(list1)) :
        char = list1[index-1]
        char2 = list1[index]
        idx = strings[charlist[char]]
        idx2 = strings[charlist[char2]]
        
    
        
        if char[:1] == char2[:1] :
            tmp.append(idx[:n]+char)
            tmp.append(idx2[:n]+char2)
            
            continue
            
        else :
            if tmp :
                tmp = set(tmp)
                tmp = list(tmp)
                tmp.sort()
                answer.extend(tmp)
                tmp = []
            else :
                answer.append(idx[:n]+char)
            

    if tmp :
        tmp = set(tmp)   
        tmp = list(tmp)
        tmp.sort()
        answer.extend(tmp)
        
    
    if answer[-1][n:] != charlast : 
        idx = strings[charlist[list1[-1]]]
        print('?', idx[:n]+list1[-1])
        answer.append(idx[:n]+list1[-1])

    return answer
```



중복된 값이 있는 경우 이를 잡아내지 못해.. 처음에 charlist로 해가지고. 그럼 어떻게 하지?

```python
def solution(strings, n):
    answer = []
    list1 = [char[n:] for char in strings]
    list1.sort()

    charlist = {char[n:] : idx for idx, char in enumerate(strings)}

    latter = list1[-1]
    tmp = []
    charlast = list1[-1]
    for index in range(1, len(list1)) :
        char = list1[index-1]
        char2 = list1[index]
        idx = strings[charlist[char]]
        idx2 = strings[charlist[char2]]
        
        
        if char[:1] == char2[:1] :
            if latter != char :
                tmp.append(idx[:n]+char)
            tmp.append(idx2[:n]+char2)
            latter = char2
            
            continue
            
        else :
            #print(char, char2, tmp, answer, idx[:n]+char)
            if tmp :
                
                tmp.sort()
                answer.extend(tmp)
                tmp = []
            else :
                answer.append(idx[:n]+char)
            

    if tmp :
        tmp = set(tmp)   
        tmp = list(tmp)
        tmp.sort()
        answer.extend(tmp)
        
    
    if answer[-1][n:] != charlast : 
        idx = strings[charlist[list1[-1]]]
        answer.append(idx[:n]+list1[-1])

    return answer
```





## 다른 사람의 풀이





## 배운 점

